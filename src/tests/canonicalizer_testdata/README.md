# Canonicalizer Testdata

PuzzleScript specimens in this directory are loaded by `../canonicalizer_node.js`.

Keep assertions in JavaScript and use these files for source programs that are large
enough to be easier to browse as standalone PuzzleScript examples.
